
from distutils.core import setup

from setuptools import setup, find_packages

setup(name='PathStroke',
      install_requires=['pyx'],
      version='0.2',
      py_modules=find_packages(),
      )
